# DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:mysql://localhost:3306/YeeShin_ProductManagement_Web_Version?serverTimezone=UTC
spring.datasource.password=Hma007@starfish
spring.datasource.username=root

# Hibrnate

# The SQL dialect makes HIbernate generate better SQL for the chosen database
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySLQ5InnoDBDialect

# Hiberante ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE



